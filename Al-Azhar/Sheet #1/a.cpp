#include <bits/stdc++.h>
using namespace std;

void doWork() {
    string s;
    cin >> s;
    cout << "Hello, " << s << '\n';
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

#ifdef ONLINE_JUDGE
#endif

    int cases = 1;
    // cin >> cases;
    while (cases--)
        doWork();
    return 0;
}
